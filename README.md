# demo-quote-cli

Tiny zero-dependency CLI that prints a random programming quote.

## Usage

```bash
python3 quote.py        # one quote
python3 quote.py -n 3   # three quotes
```

## Example

```
"Talk is cheap. Show me the code."
    — Linus Torvalds
```

MIT licensed.
